// GetSongBPM API — NOTE: blocked by Cloudflare bot protection from mobile apps.
// BPM is entered manually by the user via the track row UI in record detail screen.

export async function lookupBpm(
  _trackTitle: string,
  _artistName: string
): Promise<number | null> {
  return null;
}
